mixin text-field(options = {})
  .text-field
    if options.title
      h3.text-field__title=options.title
    input(
      class="text-field__input"
      type=options.type
      placeholder=options.placeholder
      value=options.value
      autofocus=options.isFocused
      disabled=options.isDisabled
    )