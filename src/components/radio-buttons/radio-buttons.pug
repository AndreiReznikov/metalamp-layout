mixin radio-buttons(options = {})
  -
    let i = 0;

  .radio-buttons
    while i < options.radioNames.length
      label.radio-buttons__label=options.radioNames[i++]
        input.radio-buttons__input(type="radio" name="name" checked=options.checkedNumber === i - 1)
        span.radio-buttons__custom-input